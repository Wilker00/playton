# Agent Guidelines

- Keep backend endpoints under the `/api` prefix.
- Always require JWT authentication for protected routes.
- Ensure UI components fetch live data from the backend rather than using static placeholders.
